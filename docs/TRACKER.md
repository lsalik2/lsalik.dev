# lsalik.dev tracker

## BUGS
**curl demo block flashes as one size and instantly changes**
- Issue description: when the page load, the curl demo appears as one size for a split second then decreases instantly
- Intended behavior: it should show up as one size immediately, then only resize if needed to fit all the curl result
